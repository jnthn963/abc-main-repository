name: Build and Deploy to Hostinger

on:
  push:
    branches:
      - main  # This matches the 'main' branch seen in your GitHub setup

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v4

      - name: 🛠 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: 🔨 Install dependencies
        run: npm install

      - name: 🏗 Build project
        run: npm run build

      - name: 📂 Sync files via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }} # Uses 145.79.25.189
          username: ${{ secrets.FTP_USERNAME }} # Uses u325953503.mainoffice.online
          password: ${{ secrets.FTP_PASSWORD }} # Uses your saved secret
          local-dir: ./dist/  # Vite projects build into this folder
          server-dir: ./public_html/ # Matches your Hostinger folder
